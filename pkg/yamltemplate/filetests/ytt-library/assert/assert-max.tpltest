#@ load("@ytt:assert", "assert")

max_with_int:
  as_validation_assertion: #@ assert.max(1, 2)
  as_plain_func:           #@ assert.max(1, 2)
max_with_float:
  as_validation_assertion: #@ assert.max(1.1, 1.2)
  as_plain_func:           #@ assert.max(1.1, 1.2)
max_with_string:
  as_validation_assertion: #@ assert.max("a", "b")
  as_plain_func:           #@ assert.max("a", "b")

+++

max_with_int:
  as_validation_assertion: null
  as_plain_func: null